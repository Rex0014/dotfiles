monitor = , preferred, auto, 1

exec-once = qs

env = XCURSOR_SIZE,24
env = QT_QPA_PLATFORM,wayland

$mainMod = SUPER
$terminal = foot

bind = $mainMod, Q, exec, $terminal

input {
	kb_layout = fr
	}
